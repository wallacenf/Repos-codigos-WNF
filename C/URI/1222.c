#include <stdio.h>
#include <string.h>
void main(){
    char conto[1000];
    int linhasMax,palavras,caracLinha;
    while(1){
        scanf("%i%i%i",&palavras,&linhasMax,&caracLinha);
        if(feof(stdin)) break;
        gets(conto);

    }
}